"use client";import{useState,type FormEvent}from"react";import{useRouter}from"next/navigation";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";export function PlantillaCreateForm(){const router=useRouter();const[error,setError]=useState("");const[saving,setSaving]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setSaving(true);setError("");try{const body=Object.fromEntries(new FormData(event.currentTarget).entries());const response=await fetch("/api/admin/plantillas",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const payload=await response.json()as{plantilla?:{Plantilla_Sesion_ID:string}};if(!response.ok||!payload.plantilla)throw new Error();router.replace(`/admin/plantillas/${payload.plantilla.Plantilla_Sesion_ID}`);router.refresh();}catch{setError("Revisá los datos e intentá nuevamente.");}finally{setSaving(false);}}return <form className="space-y-5" onSubmit={submit}><div className="grid gap-4 sm:grid-cols-2">{[["Nombre_Plantilla","Nombre"],["Objetivo","Objetivo"],["Grupo_Muscular_1","Grupo muscular principal"],["Grupo_Muscular_2","Grupo muscular secundario"]].map(([name,label])=><label className="space-y-2 text-sm font-semibold" key={name}>{label}<Input name={name} required={name!=="Grupo_Muscular_2"}/></label>)}</div><label className="block space-y-2 text-sm font-semibold">Notas<textarea className="min-h-28 w-full rounded-xl border border-slate-300 px-4 py-3 font-normal" name="Notas"/></label>{error?<p className="text-sm text-red-700" role="alert">{error}</p>:null}<Button disabled={saving} type="submit">{saving?"Creando…":"Crear plantilla"}</Button></form>;}
