"use client";
import { useState,type ReactNode } from "react";import type { Alumno } from "@/modules/alumnos/domain/alumno";import type { Ejercicio } from "@/modules/ejercicios/domain/ejercicio";import { AiRoutinePlanner } from "./ai-routine-planner";export function RoutineCreationOptions({students,catalog,templateForm,initialMode="template"}:{students:Alumno[];catalog:Ejercicio[];templateForm:ReactNode;initialMode?:"template"|"ai"}){const[mode,setMode]=useState<"template"|"ai">(initialMode);return <div className="space-y-6"><div className="grid grid-cols-2 gap-2 rounded-2xl border border-slate-200 bg-slate-100 p-2" role="tablist"><button aria-selected={mode==="template"} className={`min-h-11 rounded-xl font-bold transition ${mode==="template"?"bg-white text-violet-800 shadow-sm":"text-slate-600 hover:bg-white/60"}`} onClick={()=>setMode("template")} role="tab">Usar plantilla</button><button aria-selected={mode==="ai"} className={`min-h-11 rounded-xl font-bold transition ${mode==="ai"?"bg-white text-violet-800 shadow-sm":"text-slate-600 hover:bg-white/60"}`} onClick={()=>setMode("ai")} role="tab">✨ Crear con IA</button></div>{mode==="template"?templateForm:<AiRoutinePlanner catalog={catalog} students={students}/>}</div>}
