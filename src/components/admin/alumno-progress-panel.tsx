import { ProgressForm } from "@/components/progreso/progress-form";
import { ProgressMetrics } from "@/components/progreso/progress-metrics";
import { Card } from "@/components/ui/card";
import type { Ejercicio } from "@/modules/ejercicios/domain/ejercicio";
import type { RegistroProgreso } from "@/modules/progresos/domain/registro-progreso";
export function AlumnoProgressPanel({ entries }: { entries: { record: RegistroProgreso; catalog: Ejercicio | null }[] }) { return <Card className="space-y-4"><div><h2 className="text-xl font-bold">Progreso</h2><p className="text-sm text-slate-600">Historial agrupable por ejercicio, incluidos los archivados.</p></div>{!entries.length ? <p className="text-slate-600">Este alumno todavía no registró progreso.</p> : entries.map(({ record, catalog }) => <div className="space-y-3 rounded-xl border border-slate-200 p-4" key={record.Registro_ID}><div className="flex justify-between gap-3"><div><h3 className="font-black">{catalog?.Ejercicio ?? "Ejercicio no disponible"}</h3><p className="text-sm text-slate-500">{new Date(record.Fecha_Registro).toLocaleString("es-AR")}</p></div>{catalog && !catalog.Activo ? <span className="h-fit rounded-full bg-slate-100 px-2 py-1 text-xs font-bold">Archivado</span> : null}</div><p className="font-semibold"><ProgressMetrics record={record}/></p><ProgressForm endpoint={`/api/admin/progresos/${record.Registro_ID}`} initial={record} method="PUT"/></div>)}</Card>; }
