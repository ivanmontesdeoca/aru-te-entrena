"use client";
import { useMemo,useState } from "react";
import { useRouter } from "next/navigation";
import { ExerciseSearchCombobox } from "@/components/admin/exercise-search-combobox";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import type { Ejercicio } from "@/modules/ejercicios/domain/ejercicio";
import type { PlantillaSesion,PlantillaEjercicio } from "@/modules/plantillas/domain/plantilla";
type Entry=Partial<PlantillaEjercicio>&{key:string;Catalogo_ID:string;Tipo_Bloque:string;Reps_Tiempo:string;Series:string;Carga:string;Descanso:string;RIR:string;Observaciones:string};type Block={key:string;type:string;items:Entry[]};
const fresh=(id:string,type:string):Entry=>({key:crypto.randomUUID(),Catalogo_ID:id,Tipo_Bloque:type,Reps_Tiempo:"",Series:"",Carga:"",Descanso:"",RIR:"",Observaciones:""});

export function PlantillaEditor({session,composition,catalog}:{session:PlantillaSesion;composition:Array<{item:PlantillaEjercicio;catalog:Ejercicio|null}>;catalog:Ejercicio[]}) {
 const router=useRouter();const initial=useMemo(()=>{const map=new Map<number,Block>();for(const{item}of composition){if(!map.has(item.Orden_Bloque))map.set(item.Orden_Bloque,{key:crypto.randomUUID(),type:item.Tipo_Bloque,items:[]});map.get(item.Orden_Bloque)!.items.push({...item,key:item.Plantilla_Ejercicio_ID})}return[...map.entries()].sort((a,b)=>a[0]-b[0]).map(([,block])=>block)},[composition]);
 const[blocks,setBlocks]=useState(initial),[saving,setSaving]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");
 function patch(blockIndex:number,itemIndex:number,field:string,value:string){const next=structuredClone(blocks);next[blockIndex].items[itemIndex]={...next[blockIndex].items[itemIndex],[field]:value};setBlocks(next)}
 function moveBlock(index:number,delta:number){const next=[...blocks],target=index+delta;if(target<0||target>=next.length)return;[next[index],next[target]]=[next[target],next[index]];setBlocks(next)}
 function moveItem(blockIndex:number,itemIndex:number,delta:number){const next=structuredClone(blocks),target=itemIndex+delta;if(target<0||target>=next[blockIndex].items.length)return;[next[blockIndex].items[itemIndex],next[blockIndex].items[target]]=[next[blockIndex].items[target],next[blockIndex].items[itemIndex]];setBlocks(next)}
 async function save(formData:FormData){setSaving(true);setError("");setMessage("");const exercises=blocks.flatMap((block,blockIndex)=>block.items.map((item,itemIndex)=>({...item,key:undefined,Tipo_Bloque:block.type,Orden_Bloque:blockIndex+1,Orden_Ejercicio:itemIndex+1})));try{const response=await fetch(`/api/admin/plantillas/${session.Plantilla_Sesion_ID}`,{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({session:Object.fromEntries(formData.entries()),exercises})});if(!response.ok)throw new Error();setMessage("Plantilla guardada.");router.refresh()}catch{setError("No se pudo guardar. Revisá la composición y los ejercicios seleccionados.")}finally{setSaving(false)}}
 return <form action={save} className="space-y-6"><section className="grid gap-4 rounded-2xl border border-slate-200 bg-white p-5 sm:grid-cols-2">{[["Nombre_Plantilla","Nombre",session.Nombre_Plantilla],["Objetivo","Objetivo",session.Objetivo],["Grupo_Muscular_1","Grupo principal",session.Grupo_Muscular_1],["Grupo_Muscular_2","Grupo secundario",session.Grupo_Muscular_2]].map(([name,label,value])=><label className="space-y-2 text-sm font-semibold" key={name}>{label}<Input defaultValue={value} name={name} required={name!=="Grupo_Muscular_2"}/></label>)}<label className="space-y-2 text-sm font-semibold sm:col-span-2">Notas<textarea className="min-h-24 w-full rounded-xl border border-slate-300 px-4 py-3 font-normal" defaultValue={session.Notas} name="Notas"/></label></section><section className="space-y-4"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="text-2xl font-black">Bloques y ejercicios</h2><p className="text-sm text-slate-600">Los órdenes se normalizan al guardar.</p></div><Button onClick={()=>setBlocks([...blocks,{key:crypto.randomUUID(),type:`Bloque ${blocks.length+1}`,items:[]}])}>Nuevo bloque</Button></div>{blocks.map((block,blockIndex)=><article className="space-y-4 rounded-2xl border border-slate-200 bg-white p-4" key={block.key}><header className="flex flex-wrap gap-2"><Input className="min-w-48 flex-1" onChange={event=>{const next=structuredClone(blocks);next[blockIndex].type=event.target.value;setBlocks(next)}} value={block.type}/><button className="min-h-11 min-w-11" onClick={()=>moveBlock(blockIndex,-1)} type="button">↑</button><button className="min-h-11 min-w-11" onClick={()=>moveBlock(blockIndex,1)} type="button">↓</button></header>{block.items.map((item,itemIndex)=>{const master=catalog.find(x=>x.Catalogo_ID===item.Catalogo_ID);return <div className="space-y-3 rounded-xl border p-4" key={item.key}><div className="flex justify-between gap-3"><div><h3 className="font-bold">{master?.Ejercicio??"Ejercicio inexistente"}</h3><p className="text-xs text-slate-500">{master?.Tipo_de_Ejercicio}</p></div><div><button className="min-h-11 px-2" onClick={()=>moveItem(blockIndex,itemIndex,-1)} type="button">↑</button><button className="min-h-11 px-2" onClick={()=>moveItem(blockIndex,itemIndex,1)} type="button">↓</button><button className="min-h-11 px-2 text-red-700" onClick={()=>{const next=structuredClone(blocks);next[blockIndex].items.splice(itemIndex,1);setBlocks(next)}} type="button">Quitar</button></div></div><div className="grid gap-3 sm:grid-cols-3">{[["Reps_Tiempo","Reps / tiempo"],["Series","Series"],["Carga","Carga"],["Descanso","Descanso"],["RIR","RIR"],["Observaciones","Observaciones"]].map(([field,label])=><label className="text-xs font-semibold" key={field}>{label}<Input onChange={event=>patch(blockIndex,itemIndex,field,event.target.value)} value={String(item[field as keyof Entry]??"")}/></label>)}</div></div>})}<div className="rounded-xl border border-dashed border-violet-200 bg-violet-50/60 p-3"><ExerciseSearchCombobox catalog={catalog} onSelect={choice=>{const next=structuredClone(blocks);next[blockIndex].items.push(fresh(choice.Catalogo_ID,next[blockIndex].type));setBlocks(next)}}/></div></article>)}</section>{message?<p className="text-sm text-emerald-700" role="status">✓ {message}</p>:null}{error?<p className="text-sm text-red-700" role="alert">{error}</p>:null}<Button disabled={saving} type="submit">{saving?"Guardando…":"Guardar plantilla completa"}</Button></form>
}
