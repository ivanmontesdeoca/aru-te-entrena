import Link from"next/link";import{notFound}from"next/navigation";import{RutinaEditor}from"@/components/admin/rutina-editor";import{requireAdmin}from"@/modules/auth/infrastructure/current-user";import{RutinaAdminError}from"@/modules/rutinas/application/errors";import{getRutinaAdminService}from"@/modules/rutinas/infrastructure/rutina-admin-service-factory";export default async function RutinaPage({params}:{params:Promise<{id:string}>}){await requireAdmin();const{id}=await params;let detail;try{detail=await getRutinaAdminService().get(id);}catch(error){if(error instanceof RutinaAdminError)notFound();throw error;}return <main className="mx-auto max-w-5xl space-y-6 px-5 py-8"><Link className="text-sm font-semibold text-violet-700" href="/admin/rutinas">← Volver a rutinas</Link><div className="flex flex-wrap justify-between gap-3"><div><p className="text-sm font-bold uppercase tracking-widest text-violet-700">Sesión → Bloques → Ejercicios</p><h1 className="text-3xl font-black">{detail.session.Titulo}</h1><p className="text-sm text-slate-500">Origen: {detail.session.Plantilla_Sesion_Origen_ID??"Sin plantilla"}</p></div><span className="h-fit rounded-full bg-slate-100 px-3 py-1 text-sm font-bold">{detail.session.Entrenamiento_Completado?`Completado ${detail.session.Fecha_Completado}`:"Pendiente"}</span></div><RutinaEditor catalog={detail.catalog} composition={detail.exercises} session={detail.session} students={detail.students}/></main>;}
