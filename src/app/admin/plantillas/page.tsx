import Link from"next/link";import{Card}from"@/components/ui/card";import{requireAdmin}from"@/modules/auth/infrastructure/current-user";import{getPlantillaAdminService}from"@/modules/plantillas/infrastructure/plantilla-admin-service-factory";export default async function PlantillasPage({searchParams}:{searchParams:Promise<{q?:string;objetivo?:string;grupo?:string}>}){await requireAdmin();const params=await searchParams;const service=getPlantillaAdminService();const[entries,filters]=await Promise.all([service.list({search:params.q,objective:params.objetivo,muscle:params.grupo}),service.filters()]);return <main className="mx-auto max-w-6xl space-y-6 px-5 py-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-bold uppercase tracking-widest text-violet-700">Administración</p><h1 className="text-3xl font-black">Plantillas</h1></div><Link className="inline-flex min-h-11 items-center justify-center rounded-lg bg-violet-700 px-4 font-semibold text-white" href="/admin/plantillas/nueva">Nueva plantilla</Link></div><form className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 sm:grid-cols-3"><label className="text-sm font-semibold">Buscar<input className="min-h-11 w-full rounded-xl border border-slate-300 px-3 font-normal" defaultValue={params.q??""} name="q" placeholder="Nombre de plantilla"/></label><label className="text-sm font-semibold">Objetivo<select className="min-h-11 w-full rounded-xl border border-slate-300 bg-white px-3 font-normal" defaultValue={params.objetivo??""} name="objetivo"><option value="">Todos</option>{filters.objectives.map(value=><option key={value}>{value}</option>)}</select></label><label className="text-sm font-semibold">Grupo muscular<select className="min-h-11 w-full rounded-xl border border-slate-300 bg-white px-3 font-normal" defaultValue={params.grupo??""} name="grupo"><option value="">Todos</option>{filters.muscles.map(value=><option key={value}>{value}</option>)}</select></label><button className="min-h-11 rounded-lg border border-violet-700 font-semibold text-violet-700 sm:col-span-3">Aplicar filtros</button></form><div className="grid gap-4 md:grid-cols-2">{entries.map(({session,exerciseCount})=><Link href={`/admin/plantillas/${session.Plantilla_Sesion_ID}`} key={session.Plantilla_Sesion_ID}><Card className="h-full space-y-2 hover:border-violet-300"><h2 className="font-bold">{session.Nombre_Plantilla}</h2><p className="text-sm text-slate-600">{session.Objetivo}</p><p className="text-sm text-slate-500">{[session.Grupo_Muscular_1,session.Grupo_Muscular_2].filter(Boolean).join(" · ")}</p><p className="text-sm font-semibold text-violet-700">{exerciseCount} ejercicios</p></Card></Link>)}</div>{!entries.length?<Card><p className="text-slate-600">No se encontraron plantillas.</p></Card>:null}</main>;}
