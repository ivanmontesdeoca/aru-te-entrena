import Link from"next/link";import{notFound}from"next/navigation";import{PlantillaEditor}from"@/components/admin/plantilla-editor";import{requireAdmin}from"@/modules/auth/infrastructure/current-user";import{PlantillaAdminError}from"@/modules/plantillas/application/errors";import{getPlantillaAdminService}from"@/modules/plantillas/infrastructure/plantilla-admin-service-factory";export default async function PlantillaPage({params}:{params:Promise<{id:string}>}){await requireAdmin();const{id}=await params;let detail;try{detail=await getPlantillaAdminService().get(id);}catch(error){if(error instanceof PlantillaAdminError)notFound();throw error;}return <main className="mx-auto max-w-5xl space-y-6 px-5 py-8"><Link className="text-sm font-semibold text-violet-700" href="/admin/plantillas">← Volver a plantillas</Link><div><p className="text-sm font-bold uppercase tracking-widest text-violet-700">Sesión → Bloques → Ejercicios</p><h1 className="text-3xl font-black">{detail.session.Nombre_Plantilla}</h1></div><PlantillaEditor catalog={detail.catalog} composition={detail.exercises} session={detail.session}/></main>;}
