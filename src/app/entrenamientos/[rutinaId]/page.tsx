import Link from "next/link";
import { notFound } from "next/navigation";
import { AlumnoHeader } from "@/components/alumno/alumno-header";
import { CompletionButton } from "@/components/alumno/completion-button";
import { Card } from "@/components/ui/card";
import { getAuthenticatedAlumnoId } from "@/modules/auth/infrastructure/current-user";
import { EntrenamientoAlumnoError } from "@/modules/entrenamientos/application/errors";
import { getEntrenamientoAlumnoService } from "@/modules/entrenamientos/infrastructure/entrenamiento-alumno-service-factory";
function Value({ label, value }: { label: string; value: string }) { return value ? <div><dt className="text-xs font-bold uppercase tracking-wide text-slate-500">{label}</dt><dd className="font-semibold text-slate-800">{value}</dd></div> : null; }
export default async function EntrenamientoDetallePage({ params }: { params: Promise<{ rutinaId: string }> }) { const alumnoId = await getAuthenticatedAlumnoId(); const { rutinaId } = await params; let detail; try { detail = await getEntrenamientoAlumnoService().detail(alumnoId, rutinaId); } catch (error) { if (error instanceof EntrenamientoAlumnoError) notFound(); throw error; } const { session, blocks } = detail; return <><AlumnoHeader /><main className="mx-auto max-w-3xl space-y-6 px-4 py-6 sm:px-6 sm:py-10"><Link className="font-bold text-violet-700" href="/entrenamientos">← Mis entrenamientos</Link><header className="space-y-3"><p className="text-sm font-bold uppercase tracking-widest text-violet-700">{session.Dia_Entrenamiento_Semana}</p><h1 className="text-3xl font-black">{session.Titulo}</h1><p className="text-slate-600">Fecha planificada: {session.Fecha}</p>{session.Notas_Generales ? <p className="rounded-xl bg-violet-50 p-4 text-slate-700">{session.Notas_Generales}</p> : null}<p className={`font-bold ${session.Entrenamiento_Completado ? "text-emerald-700" : "text-amber-700"}`}>{session.Entrenamiento_Completado ? `Entrenamiento completado · ${session.Fecha_Completado}` : "Entrenamiento pendiente"}</p></header><CompletionButton completed={session.Entrenamiento_Completado} rutinaId={session.Rutina_Sesion_ID} />{!blocks.length ? <Card><p className="text-slate-700">Este entrenamiento todavía no tiene ejercicios cargados. Consultá con tu entrenadora.</p></Card> : null}{blocks.map((block) => <section className="space-y-3" key={block.order}><h2 className="text-xl font-black">{block.name}</h2>{block.exercises.map(({ item, catalog }, index) => <Card className="space-y-4" key={item.Rutina_Ejercicio_ID}><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold text-violet-700">Ejercicio {index + 1}</p><h3 className="text-xl font-black">{catalog?.Ejercicio ?? "Ejercicio no disponible"}</h3><p className="text-sm text-slate-500">{catalog?.Tipo_de_Ejercicio}</p></div>{catalog && !catalog.Activo ? <span className="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold text-slate-600">Archivado</span> : null}</div><dl className="grid grid-cols-2 gap-3 sm:grid-cols-3"><Value label="Series" value={item.Series}/><Value label="Reps / tiempo" value={item.Reps_Tiempo}/><Value label="Carga" value={item.Carga}/><Value label="Descanso" value={item.Descanso}/><Value label="RIR" value={item.RIR}/><Value label="Alternativa" value={item.Instrumento_Alternativo}/></dl>{item.Observaciones ? <p className="border-t border-slate-100 pt-3 text-sm"><strong>Indicaciones:</strong> {item.Observaciones}</p> : null}{catalog?.Aclaraciones ? <p className="text-sm"><strong>Aclaraciones:</strong> {catalog.Aclaraciones}</p> : null}<div className="flex flex-wrap gap-2">{catalog?.Video ? <a className="rounded-lg border border-violet-300 px-3 py-2 text-sm font-bold text-violet-700" href={catalog.Video} rel="noopener noreferrer" target="_blank">Ver video</a> : null}{catalog?.Video_Adicional ? <a className="rounded-lg border border-violet-300 px-3 py-2 text-sm font-bold text-violet-700" href={catalog.Video_Adicional} rel="noopener noreferrer" target="_blank">Ver video adicional</a> : null}</div></Card>)}</section>)}</main></>; }
